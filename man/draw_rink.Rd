% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_rink.R
\name{draw_rink}
\alias{draw_rink}
\title{Draw an NHL rink}
\usage{
draw_rink(view = "full", orientation = "horizontal")
}
\arguments{
\item{view}{character rink view: \code{'full'} (\code{'f'}), \code{'offensive-half'}
(\code{'oh'}), \code{'defensive-half'} (\code{'dh'}), \code{'offensive-zone'} (\code{'oz'}),
\code{'defensive-zone'} (\code{'dz'}), or \code{'neutral-zone'} (\code{'nz'})}

\item{orientation}{character orientation: \code{'horizontal'} (\code{'h'}) or
\code{'vertical'} (\code{'v'})}
}
\value{
A \code{ggplot2} plot.
}
\description{
\code{draw_rink()} creates a regulation NHL rink that can be extended with
\code{ggplot2} layers. The centered coordinate system matches the coordinate
fields returned by \code{nhlscraper}.
}
\details{
The offensive end is on the right for a horizontal rink and at the top for
a vertical rink. Use \code{xCoordNorm} and \code{yCoordNorm} from
\code{\link[nhlscraper:gc_pbp]{nhlscraper::gc_pbp()}} when every event should attack toward that end. Raw
play-by-play and replay coordinates remain spatially aligned but retain
their physical direction unless normalized by the caller.

Both orientations use the same layer mappings: map longitudinal coordinates
to \code{x} and lateral coordinates to \code{y}. On a vertical rink, positive lateral
coordinates appear on the right.
}
\examples{
draw_rink()
draw_rink(view = 'oz', orientation = 'v')

}
\references{
\href{https://media.nhl.com/site/asset/public/ext/2025-26/2025-26Rules.pdf}{National Hockey League Official Rules 2025-2026}
}
